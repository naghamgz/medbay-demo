/**
 * Site-wide demo configuration.
 * Flip these values to change site behavior without touching any page markup.
 */
window.SITE_CONFIG = {
  demoMode: true,
  defaultLanguage: "en",   // "en" | "ar"
  defaultCurrency: "AED",  // AED | USD | SAR | EGP
  paymentsEnabled: false,  // when true, payment-adapter.js should call a real provider instead of the demo modal
};
