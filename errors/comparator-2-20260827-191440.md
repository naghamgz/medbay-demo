# Comparator error — section_index 2 (main wrapper)

## Root cause

No reference screenshot exists for `section_index: 2` and none can meaningfully be
produced, so the mandated pixelmatch comparison (Step 10c) cannot run.

1. **No extractor screenshot was captured for this index.** `references/screenshots/`
   contains `1440-section-0-sticky.png`, `1440-section-1-flex.png`,
   `1440-section-3-relative.png`, `1440-section-4-wrapper.png`, ... `1440-section-17-*`
   — the sequence jumps from `section-1` straight to `section-3`. Confirmed via:
   `ls references/screenshots | grep -i "section-2[^0-9]"` → no match (exit code 1).
   This is consistent with `site-dna.json`'s flat `layout['1440px'].sections` array,
   where index 2 is `<main class="relative isolate">` — a DOM landmark, not a
   visually distinct, independently-screenshotted section.

2. **The built artifact has no renderable visual content to screenshot.** Per
   `build/sections/02-main/notes.md` and `agent-log-reconstructor-20260827-191337-section2.md`,
   the reconstructor deliberately built `02-main/index.html` as an empty shell:
   `<main class="site-main">` + one inert `.site-main-decor` div (no confirmed
   background/gradient — DNA captured only its bounding rect, no color/asset data)
   + a stub `<div class="site-main-slot" data-slot-sections="3-16">` marking where
   the real content (sections 3–16, each built independently) mounts at final
   assembly. Screenshotting `02-main/index.html` in isolation today would capture a
   blank page (0-height content region, since `.site-main`'s height is intentionally
   NOT hardcoded and no children are mounted) — a diff against any reference crop
   would be ~100% mismatch and would not reflect a real reconstruction defect.

## State of partial files

- No `clone.png`, `report.json`, `report.md`, or diff image were produced for this
  index — attempting the screenshot/pixelmatch step was skipped rather than run
  against a non-existent or misleading reference.
- `build/sections/02-main/{index.html,styles.css,notes.md}` exist and are unaffected.

## Suggested recovery action

Do not re-dispatch reconstructor for section_index 2 on visual grounds — there is no
deviation to fix; the shell was built correctly per its own scope (landmark +
decorative div only, no content duplication, matching the `00-header`/`01-nav`
dedupe precedent already validated in `comparison-report-section-1.md`).

Recommended orchestrator action:
1. Skip section_index 2 in the per-section visual QA loop (Steps 10b–10d) — treat it
   as structural-only, already reviewed via the reconstructor's own notes.
2. Proceed directly to dispatching reconstructor for `section_index: 3` (hero), the
   first real content section under `<main>`.
3. Defer visual validation of `.site-main-decor`'s appearance (flagged as
   "unresolved visual content" in `02-main/notes.md`) to the Step 12 full-page audit,
   once sections 3–16 are assembled inside `02-main` and a true full-page screenshot
   diff is possible.
